# AstronomyWeb · 每日增量计划 · 2026-08-26

> Day 3 · Phase 0 收尾:md_to_astro_db 修复 + 闭项

## 上下文

- `.plan/20260825.md` 缺失(T1 cron 昨日未产出),本 plan 由 T3 节奏自动续上
- 0824 plan 0.1 步骤(抽取脚本)实际完成但 git 未 commit,0826 巡检报告也指出"1 modified + 1 untracked"
- 0826 巡检实测发现另一个真 bug:`table_pos` 用 `"|".join(header)` 查找,无法命中含空格的 markdown 表行(如 `| 分支 | 说明 |`),导致所有 branch 都被 fallback 为"未分类"

## 今日项(1 个小变更)

- [x] **修 `extract_branches` 的 `table_pos` 查找 bug**
  - 改前:`sec_with_h2.find("|".join(header))` 永远 -1(join 出的串 "分支|说明|代表" 与表行 "| 分支 | 说明 |" 不匹配)
  - 改后:用正则 `\|\s*分支\s*\|\s*说明\s*\|...` 命中表头,精确算 table 在段内偏移
  - 结果:`branch` 分类从全部"未分类" → `核心分支 8 / 核心领域 23`(`master=3, quote=4` 保持)

## 涉及文件

- 改 `scripts/md_to_astro_db.py`(extract_branches 内,5 行)
- 重生 `data/astro_entities.json`(category 字段全部正确)
- 改 `项目开发计划.md` Phase 0 #1 → [x] 闭项
- 新增 `天文顾问开发架构与计划.md`(untracked→tracked,作为 v1.1 架构并入,但保留 `项目开发计划.md` 为唯一勾选主计划)

## 验收

- 跑 `python3 scripts/md_to_astro_db.py` → `total=38, by_type={branch:31, master:3, quote:4}`,branch 分类分布合理
- 主计划 Phase 0 #1 标记为 [x]
- 巡检已知项"1 modified + 1 untracked"清零

## 不做什么

- 不动 06 master 抽取正则(下一天处理,目标 master≥5)
- 不入库 SQLite(Phase 0 后续步骤)
- 不动 `README.md` / Phase 0 后 5 项(下一轮)
- 不合 `天文顾问开发架构与计划.md` 与 `项目开发计划.md`(命名分裂 AstronomyAdvisor vs AstroAdvisor,巡检已挂"待合并"标签,T4 阶段统一)

## 风险/备注

- 0825 plan 缺失导致节奏断档 2 天;若 T1 cron 仍未恢复,0827 由 T3 自动续
- 06 master 仅 3 条(伽利略/牛顿/爱因斯坦),原 md 应有 5+(哥白尼/开普勒/第谷/哈勃等);推测正则 `^[一二三四五六七八九十]+、(.{2,8})[：:]` 对"一、哥白尼:xxx"这种短名段的姓名截取可能落到 "哥白尼:xxx" 整段。下一轮 plan 优先处理

<!-- AUTO-GENERATED v0.1 DO-NOT-EDIT-MANUALLY -->
